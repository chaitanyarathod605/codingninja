import sys

def duplicateNumber(arr, n) :
    for i in range(0, len(arr)):
        for j in range(0, len(arr)):
            if(arr[i] == arr[j]) and i!=j:
                return arr[i]
                
 output 
 1 <= t <= 10^2
0 <= N <= 10^3
Time Limit: 1 sec
