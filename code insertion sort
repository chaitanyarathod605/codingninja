from sys import stdin

def insertionSort(arr, n) :  
    #Your code goes here
    for i in range(1,n):
        if(arr[i]<arr[i-1]):
            k=i
            while arr[k]<arr[k-1] and k>0 :
                arr[k-1],arr[k]=arr[k],arr[k-1]
                k-=1
    return
    
    
    output - 
1 <= t <= 10^2
0 <= N <= 10^3
Time Limit: 1 sec
